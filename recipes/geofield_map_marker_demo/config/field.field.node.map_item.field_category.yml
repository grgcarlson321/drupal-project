langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_category
    - node.type.map_item
    - taxonomy.vocabulary.location_category
id: node.map_item.field_category
field_name: field_category
entity_type: node
bundle: map_item
label: Category
description: 'A taxonomy term from location_category used to group this location. Displayed as the preTitle on cards and in map InfoWindows.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      location_category: location_category
    sort:
      field: name
      direction: asc
    auto_create: true
    auto_create_bundle: ''
field_type: entity_reference
